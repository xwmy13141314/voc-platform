# 修改变更记录（CHANGELOG）

> 本文件记录 VoC 痛点平台自 MVP 以来的全部版本变更。
> 版本号唯一来源：`version.py → APP_VERSION`；数据库结构版本见 `SCHEMA_VERSION`。

---

## v1.2.0（2026-09-01）— 数据质量精修 + 中文体验 + 版本治理

### 链接垃圾过滤强化（quality_filter / crawler / 前端设置页）
- 新增 `link_spam` 过滤规则：原文含 URL 且满足其一即拦截导购/带货评论
  - ① 清洗后残留有效文本少于 `link_spam_min_words`（默认 8，设置页可调）
  - ② 命中促销话术模式（buy … here + URL、use code、% off、official store、links to the best 等正则）
- 促销话术对清洗文本与原始文本分别匹配，容忍表情符号分隔（如 "Here: 🔥 https://…"）
- 全库重算效果：拦截 16 条导购评论（升级前仅 4 条），含 URL 的真实用户评论 0 误伤
- 设置页新增「链接垃圾过滤」开关与阈值输入，修改后可一键全量重算

### 聚类簇中文命名（clustering）
- 内置 150+ 三防手机赛道领域词库（英文关键词 → 中文），LLM 不可用时的降级命名自动翻译
- 品牌/型号名（Unihertz、Ulefone Armor 等）有意保留英文，便于内部对照
- 79/79 簇已应用中文降级命名（如「实体键盘 · blackberry passport · 全键盘」「夜视功能 · 热成像 · 热成像相机」）

### LLM 提供商（llm_provider）
- GLM 模型列表新增 `glm-5.2`、`glm-5.3`（智谱最新旗舰，排在下拉首位）
- DPAPI 密文跨 Windows 账户解不开时，明确提示「密文由其他系统账户加密存储，请重新录入」，不再误报为「Key 无效或已过期」
- 429 限流错误透出智谱业务错误码具体原因：1302 频率/并发超限、1305 服务端拥挤、1308 用量上限（含重置时间）、1309 Coding Plan 到期
- 瞬时限流（1302/1305）自动等待 3 秒重试一次

### 统计准确性（advisor）
- 簇内评论统计查询增加 `filtered = 0` 条件，已过滤的广告评论不再污染聚类统计与报告

### 版本治理与工程清理（本次）
- 新增本 CHANGELOG.md；README / README_EN / PRD 版本同步至 v1.2.0（PRD v1.3）
- 发布 `release-v1.2/`（PyInstaller 6.21 + Python 3.12，exe 约 44MB，种子库含全量数据：6390 评论 / 588 分析 / 79 簇 / 924 已过滤）
- **桌面精简版策略**：spec 显式排除 torch / sentence-transformers / umap / hdbscan 等约 1GB+ 科学计算栈；冻结版仍可查看簇、AI 重命名，仅「重跑聚类」需源码模式（入口返回友好提示而非崩溃）
- **空库自动播种**：首次启动若正式数据目录存在但为空库（如从无种子目录跑过一次），自动用发布包种子库覆盖，修复「先跑过一次导致种子永不生效」的坑
- 清理 v0.7/v0.8 历史构建产物（build-\*/dist-\*/release-v0.7\* 约 700MB）、损坏旧 venv（.venv-old）与无效 wheelhouse
- 全量数据已备份至 `backup-data-20260901/`（活库 + v0.8 种子库快照 + 历史测试小库 + 旧版 PRD）

---

## v1.1.0（2026-09-01）— v2.0 升级 Phase 2：全局痛点聚类引擎（W2-1）

- **BGE-M3 多语言向量化**：`BAAI/bge-m3` 本地缓存离线加载（`local_files_only` 防联网挂起），中文/英文/荷兰语等评论统一向量空间
- **embedding 增量缓存**：向量按内容哈希缓存于库，二次聚类分钟级完成（5180 条增量计算仅约 13 秒）
- **UMAP 降维 + HDBSCAN 聚类**：5480 条可聚类评论 → 79 个主题簇（64% 归簇率，噪声 1956 条）
- **簇命名**：LLM 批量语义命名（中文主题名 + 英文 slug + 描述 + 关键词）+ 无 Key 时关键词降级命名（停用词过滤）
- **「重新命名」模式**：不重跑 embedding/聚类，仅对现有簇重新执行命名（LLM 可用时覆盖占位名）
- **前端「痛点聚类」页**：聚类参数（最小簇规模/归簇阈值）、全量/增量/重命名三种模式、簇卡片（评论数/严重度/情感/关键词/簇内评论钻取）
- **环境修复**：cp312 错标 wheel 导致 openai SDK 损坏 → 重建 venv 与 wheelhouse；LLM 不可用时的高频词提取增加停用词过滤

---

## v1.0.0（2026-09-01）— v2.0 升级 Phase 1：数据质量与抓取强化（W1-1 / W1-2 / W1-6）

- **评论质量过滤引擎（W1-6）**：落盘前可配置规则闸门——`min_words` 有效长度下限（CJK 按字符/拉丁按词）、`emoji_only` 纯表情、`link_only` 纯链接、`min_likes` 按平台点赞阈值（Reddit 默认 1）、`deep_reply` 楼中楼深度标记与质量分扣减；被过滤评论仅标记 `filtered=1 + filter_reason`，不删除，阈值调整后一键全量重算；质量分（长度/点赞/深度启发式）供排序参考
- **Reddit OAuth 加固（W1-1）**：限流响应头解析、429 指数退避、移除失效匿名分支、保留评论楼中楼血缘
- **AliExpress 电商评论接入（W1-2）**：平台注册、评论质量过滤端点、前端设置页
- **Schema v4 迁移** + 冒烟测试；6390 条评论 API 端到端验收通过

---

## v0.9.1（2026-08）

- v0.8 后续稳定性补丁：健康检查增加 jiter 原生运行时检测（识别损坏 wheel 引起的 openai SDK 故障）、源码运行实例 token（source-run）防误连

---

## v0.8.0（2026-08-01）— 单机稳定版

- 统一发布目录 `release-v0.8/`；修复 Python 3.12 原生依赖打包、旧后端误连接、数据库目录混用、抓取失败误报
- 发布包种子库：6390 条评论、588 条分析、83 个视频/帖子

---

## v0.7.x（2026-07-31）

- 后台任务系统：任务表/轮询/历史/去重/取消/应用重启中断恢复
- 抓取进度细化到品牌/帖子，记录搜索与评论失败原因
- AI 结构化输出：JSON 严格枚举校验、截断/Markdown 容错、失败重试、人工修正 API 与前端表单
- Windows DPAPI 加密 API Key（兼容旧明文自动迁移）
- 9 家默认品牌、benchmark 类型支持、来源链接修复
- Reddit 数据 ID 命名空间修复（`reddit:<external_id>`）与来源链接补齐（redditfix）

---

## v0.5.0（2026-07）— MVP

- YouTube（yt-dlp）/ Reddit（JSON+RSS）评论抓取 + 多 LLM（Gemini/DeepSeek/GLM/Kimi/通义）结构化痛点分析 + SQLite 存储 + ECharts 可视化看板 + AI 改良建议报告

---

## 待办（v2.0 升级计划后续阶段）

- Phase 3（W3）：程序化统计层、建议生成器证据链 Schema、优先级量化、新产品机会挖掘、报告版式升级
- Phase 4（W4）：证据链钻取 UI、流程自动串联、数据链路升级
- 数据源扩展（W1-3/4/5/7）：Amazon、Twitter/X、Kickstarter、TikTok
